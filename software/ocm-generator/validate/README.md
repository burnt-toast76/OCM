# validate

Bounds checking, safety PL union, reach/collision feasibility.

If the plan asks 6.0 N.m from a 5.0 N.m tool, or the declared safety circuit does not meet a
module\x27s declared PLd, **the build FAILS**.

That refusal is a bigger selling point than the code generation.
