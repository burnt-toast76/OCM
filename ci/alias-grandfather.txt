# Frozen grandfather inventory for ci/check_alias_drift.py: the content-hash
# ids of every alias-spelling claim in THIS repository's store.
#
# It is empty, and that is the honest state. Every id it used to hold (383 of
# them, from the FS-N41N/FS-N40/LR-X/NEO entries) belongs to a document that
# moved to the production corpus, so the inventory travelled with the claims
# it names: those ids now live in that repository's ci/alias-grandfather.txt
# and are checked there. The guard's own rule for a user-directed entry
# removal is exactly this -- regenerate in the same commit as the removal --
# because a grandfathered id with no claim behind it can no longer detect
# store surgery, which is the property the inventory exists to defend.
#
# The reference fixtures that remain here carry no alias spellings at all, so
# nothing is listed below. It grows only in a promotion commit (see the
# check's docstring), and only for claims that live in this repository.
